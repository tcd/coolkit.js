export type WriteJsonFileOptions = {
    /**
     * Pretty format JSON output.
     * @default true
     */
    pretty?: boolean
    /**
     * Log out `path` after writing.
     * @default true
     */
    log?: boolean
}
